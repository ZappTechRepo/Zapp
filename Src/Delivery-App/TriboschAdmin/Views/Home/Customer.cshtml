@model IEnumerable<TriboschAdmin.Customer>

@{
    ViewBag.Title = "Product";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "CustomerCreate")
</p>


@using (Html.BeginForm("Customer", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    
}
<div class="panel-body">
    <div class="dataTable_wrapper">
        <table class="table table-striped table-bordered table-hover" id="dataTables-example">
            <thead>
                <tr>
                    <th class="max-width-table">
                        @Html.DisplayNameFor(model => model.CustomerName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ContactNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Email)
                    </th>
                    <th class="max-width-table">
                        @Html.DisplayNameFor(model => model.Addresses.First().Address1)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Addresses.First().Address2)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Addresses.First().Address3)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Addresses.First().PostalCode)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Addresses.First().GPSCode)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
               @foreach (var item in Model)
               {
                <tr>
                    <td class="max-width-table">
                        @Html.DisplayFor(modelItem => item.CustomerName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ContactNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td class="max-width-table">
                        @Html.DisplayFor(modelItem => item.Addresses.First().Address1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Addresses.First().Address2)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Addresses.First().Address3)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Addresses.First().PostalCode)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Addresses.First().GPSCode)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "EditCustomer", new { id = item.CustomerID }) |
                        @*@Html.ActionLink("Details", "Details", new { id = item.CustomerID }) |*@
                        @Html.ActionLink("Delete", "CustomerDelete", new { id = item.CustomerID })
                    </td>
                </tr>
               }
      
            </tbody>
        </table>
    </div>
    <!-- /.table-responsive -->
    
</div>
